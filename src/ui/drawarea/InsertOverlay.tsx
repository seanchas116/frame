import * as React from 'react'
const styles = require('./InsertOverlay.css')

export class InsertOverlay extends React.Component {
  render () {
    return <div className={styles.InsertOverlay}>
    </div>
  }
}
